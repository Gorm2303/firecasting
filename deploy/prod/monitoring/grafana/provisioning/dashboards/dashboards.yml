apiVersion: 1
providers:
  - name: 'Firecasting Prod'
    orgId: 1
    folder: 'Firecasting'
    type: file
    allowUiUpdates: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
